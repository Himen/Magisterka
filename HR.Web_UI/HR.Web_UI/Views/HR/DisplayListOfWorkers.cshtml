@model HR.Web_UI.Models.ViewModels.WorkersListViewModel

<p>
    @Html.ActionLink("Dodaj nowego pracownika", "Create")
</p>
<table class="table">
    <tr>
        <th>
            <label>Imię</label>
        </th>
        <th>
            <label>Nazwisko</label>
        </th>
        <th>
            <label>Data Urodzenia</label>
        </th>
        <th>
            <label>Zatrudnienie</label>
        </th>
        <th>
            <label>Stanowisko</label>
        </th>
        <th>
            <label>Przełożony</label>
        </th>
        <th>
            <label>Dział</label>
        </th>
    </tr>

@foreach (var item in Model.Workers)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Surname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateOfBirth)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateOfBirth)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateOfBirth)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Manager.FirstName) +" "+ @Html.DisplayFor(modelItem => item.Manager.Surname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateOfBirth)
        </td>
        <td>
            @Html.ActionLink("Edytuj", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Szczegóły", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Usuń", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
